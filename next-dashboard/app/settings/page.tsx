import { PlaceholderPage } from "@/components/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="設定"
      description="FinMind API 權杖只由後端環境變數管理，前端不提供權杖輸入欄位。"
    />
  );
}
